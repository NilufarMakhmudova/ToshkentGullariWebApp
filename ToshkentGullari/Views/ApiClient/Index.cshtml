@model ToshkentGullari.SoapServiceReference.ProductModel[]
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> @foreach (var item in Model)
    {
        <div>
            @item.BusinessCode, <strong>@item.Name</strong>, @item.Price, @item.ProductDescription
        </div>
    }

  
    </div>
</body>
</html>
